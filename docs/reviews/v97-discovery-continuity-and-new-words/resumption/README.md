# V97 resumption on Linux

Valid until: a bound runtime, content or test input changes — then repeat affected checks.

On2026-09-22 the owner requested testing and continuing the next version using the latest
documentation and the Windows handoff. Local and remote main both resolve to V96
`fd3ca7c78b6bee774c283d873d400abf4a8e7d65`. An existing V97 worktree at `442dd7c`
contains reviewed, installed but uncommitted content and incomplete integration. Its
27 inherited gate-input hashes and all65 kickoff/139 lane archives remain exact.
The task resumes this work instead of creating a duplicate version.

No arcade-specific new Windows skill page was located in the standard skill directories,
repo records or arcade vault notes. Recent Windows producer/decision-lab handoffs concern
another repository. The user was asked for the intended skill name/path; current work
uses this repository's STATUS, ADR0113/0155, critique rubric and recorded V97 reviews.
No unsourced skill instruction or external handoff is claimed adopted.

## Recovered blockers

The inherited complete browser run passed485 and failed67. Sixty-six Alchimie failures
share one contract mismatch: backend compatibility accepts16 histories, but the browser
still rejected a ninth. That makes a fresh saved collection fail on its next owned write.
The browser bound now matches16; native checks retain17 rejection, exact lowercase unique
hashes,64KiB save size,256 discoveries, ownership and private recipe exclusion. Nine/sixteen
book round-trips and a real bundled-catalog regression protect this boundary.

The remaining mobile Lanț failure was reproduced separately and with both real and
simulated clocks. Expiring the56px outer notification row moved an already partly clipped
persistent replay error wholly above the viewport. A layout effect now reveals the full
paragraph once when failure appears; it does not focus anything or replay a request.
Shared browser checks require the entire error within the game scrollport before and
after expiry. A test initially sampled focus before the pending render disabled replay;
its setup now waits for the real disabled state before comparing focus after failure.

All inherited backend/data/script/test inputs remain unchanged during these frontend
fixes. The earlier source-bound content approvals remain valid; old browser/static evidence
is historical and requires the new build's complete browser verification.

## Version state

The complete assembled candidate passes **2,329 backend and 53 account tests on each of
Python 3.12/3.14**, **214 native frontend and 552 browser checks**, with two browser workers
and zero retries. Validators, Ruff, lint, typecheck, build, docs and whitespace are green;
the initial bundle is 119.23/120 KiB. Focused evidence includes 93 V97 backend, 19 browser-save
native checks, 84 Alchimie browser cases and 26 shared start/replay cases.

The final integration receipt binds 522 current runtime, data, source and test inputs. The
original 65 kickoff/139 integration archives remain unchanged. [Development evidence](development-archive.json)
retains the inherited work snapshot, original 67 browser failures, isolated notice failure,
new test synchronization failures and passing correction logs. [Independent acceptance](independent-review.json)
checks 34 source/static bindings and 87 evidence hashes. Final gate logs are under `gates/`.

V97 remains a local candidate on its existing branch. Shared main stays clean. No merge,
push, deployment or recurring loop restart was performed. The owner clarification about
finishing V97 versus landing it and starting V98 remains pending, as does the exact Windows
skill name/path. No new version number or skill workflow is invented to fill that gap.
