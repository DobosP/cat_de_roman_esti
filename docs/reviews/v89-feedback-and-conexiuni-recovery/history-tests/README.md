# V89 history and public Mop checks

Valid until: a later content or test change — then treat as history.

The first focused pytest run after artifact/runtime freeze passed all **266 checks** in
74.65 seconds, including eight new household-content cases. [Verification](verification.json)
binds the tested sources and the lossless logs; this development run does not replace the
full integration matrices. Ruff and whitespace checks passed. One initial lint observation
was an overlong test assertion; it was wrapped before pytest ran.

The new cases reconstruct all four exact V88 artifacts, reject unreviewed header and old-row
changes, preserve all 658 old pack records (including 83 Alchimie and 100 Lanț records) and
336 complete derived rows, and keep the graph/mobile bytes unchanged. They assert only
`ct_viata_de_roman_353` (Mop) is newly public. Făraș and Aspirator remain rejected in the
three-input final gate; the original candidate, two final reviewers and exact dossiers remain
bound. The public Mop journey checks a safe clue, Praf feedback, free repeat, resume and exact win.

The manually reviewed [current measurements](measured-pins.json) leave the KG build at V88.
The checked-in history helper peels the exact V89 native Diplomat cue and dust scopes before
older feedback-history checks; the V88 bread test keeps its original exact addition assertion.

Receipt generation: [artifact inverse generator](generate_delta.py.txt) and
[current measurement script](measure_pins.py.txt). These scripts change no served artifacts.

## Historical topology correction after the first full backend

The first full Python 3.12 run exposed one additional historical assertion: newly public Mop
was included in the old mature-target set, although Mop was already reachable inside the
old cleaning component. The [focused reproduction](v44-topology-initial.json) failed once
in 2.61 seconds; its exact failure log remains archived.

After that full run exited, the [test-only correction](v44-topology-corrected.json) restored
the exact 235 V88 targets from the bound pack and ranking inverses. All 236 current targets
still must be reached by all proxy anchors, and the unchanged 71-proxy hash is still exact.
Mop is the sole new target; exactly the same six cleaning proxies reach it before V88, now,
and after removal of the V88 bridge. Native ownership, self-distance and its Podea proxy
are also checked. No runtime, data or frontend file changed for this correction.

All **110 focused checks passed in 18.67 seconds**, including the complete V44 file, V89
content/feedback journeys and related historical reconstructions. Ruff and whitespace passed.
The exact patch and all three command logs are losslessly archived with hashes; fresh full
backend matrices remain the orchestrator's separate integration gate.
