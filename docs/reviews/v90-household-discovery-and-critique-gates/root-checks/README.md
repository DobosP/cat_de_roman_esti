# V90 supported transactions and final runner

Valid until: V90 is superseded — then treat as history.

[Original logs](logs.json) preserve the successful read-only graph preflight, supported
graph application, raw import (two pending/one skipped), exact dossiers, portable gate,
two promotions and serial sidecar regeneration. Graph apply completed before the next
content mutation. Native Praf ownership and both review hashes were verified; the root's
initial field-name/assertion control error is disclosed in
[its separate correction](../root-binding-check-correction.json).

Interim rubric and staged content required coherent ranking/derived metadata and default
pins before BFF review; freshness guards stayed strict. All artifacts were generated by
the supported tools. The final seeded starts change only Contexto reachable_count.
Fresh constrained Python3.14 installation output is retained.

[The final runner](run-gates.py.txt) adds periodic real progress messages after an earlier
process ended incomplete; test commands, assertions and limits stay unchanged. Its original
wrapper/log/freeze are in [incomplete integration](../incomplete-integration/README.md).
[The freeze checker](check-freeze.py.txt) protects runtime/tests/rubric and every existing
non-Markdown review input at matrix start. Later new audit receipts may be added, while
all original protected inputs must still match. Static build outputs are separately bound
by the final manifest and successful frontend build.
