# constraints.txt — pinned versions for REPRODUCIBLE deploys of the `[web]` extra.
#
# pyproject.toml keeps deliberately loose ranges (fastapi>=0.110, uvicorn>=0.29) so
# local dev can float forward. This file pins the EXACT resolved closure of `.[web]`
# (fastapi + uvicorn + their transitive deps, incl. pydantic/starlette) so a Docker
# build months from now installs byte-for-byte the same wheels. The Dockerfile applies
# it with `pip install -c constraints.txt ".[web]"`.
#
# Runtime is otherwise standard-library only (the vendored roedu_client uses urllib;
# the engine/graph are pure stdlib). These pins exist ONLY for the optional web server.
#
# Regenerate (from a clean resolver) with:
#   pip install --dry-run --ignore-installed --report r.json "fastapi>=0.110" "uvicorn>=0.29"
#   # then pin every name==version from r.json["install"]
#
# Resolved 2026-06-21 against PyPI for CPython 3.11 (the python:3.11-slim runtime).

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
click==8.4.1
fastapi==0.138.0
h11==0.16.0
idna==3.18
pydantic==2.13.4
pydantic-core==2.46.4
starlette==1.3.1
typing-inspection==0.4.2
typing-extensions==4.15.0
uvicorn==0.49.0
