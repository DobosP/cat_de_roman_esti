# V90 graph, migration and history checks

Valid until: a bound source or artifact changes — then treat as history.

The first focused pytest run after final server artifact/runtime freeze passed **367 checks
in 185.36 seconds**, across 20 files, including all **53 new V90 cases**. There were no pytest
failures. [Verification](verification.json) binds 46 source files and all lossless archives;
this focused run does not replace the full backend/browser integration matrices. Final
Ruff and whitespace checks passed. Two initial installed-file import-order findings were
fixed before pytest; their exact output remains archived.

## Current V90 behavior

The new checks bind both independent graph reviews, test all 17 directed Lanț actions with
real reverse-edge controls, and verify three native owners and all 10 stored grammatical
forms under case/diacritic variants, one-attempt deduplication and exact self-wins. All 13
old-node degree changes are literal before/after pairs; every other old field, alias,
edge and puzzle remains exact.

Native dust has one identity, gives direct nonwinning feedback for four cleaning tools,
and stays distinct from named food powders and earth. Advisory typo help hides the target;
a confident typo asks before a non-target charge and wins its actual owner without first
revealing it. A custom graph missing native dust receives no retired synthetic fallback.
The other 464 projection payloads, 26 domains, 71 proxy mappings, 11 native pairs and four audit
tuples remain covered by the new and historical tests.

Public Făraș 355, Aspirator 356 and existing Mop 353 journeys exercise ordinary clues,
useful inputs, privacy, free repetition, resume and exact wins. Only 355/356 are new records;
their two final judgments remain independently bound, and Burete's raw drop remains held.
All 659 old pack records, including 83 Alchimie and 100 Lanț records, and 336 complete derived rows
are preserved. Exactly seven old kitchen nodes gain paths to the 235 mature V88 targets;
removing only the reviewed Farfurie→Burete direction restores the original six cleaning
nodes' access. The proxy map itself is unchanged.

## Historical boundaries

[The five-artifact inverse](../artifact-delta.json) reconstructs exact V89 KG, pack, rankings,
derived catalog and mobile bytes, with old-row/header tamper rejection. It extends every
older inverse; V89's mobile fingerprint remains `d2fbb9f550a05b6b128431ee55787b2b156846887f0bc8ce1908f09e6683b951`.

The [scoped V89 fixture](../../../../tests/v89_contexto_snapshot.py) restores the exact old
KG, all 465 projection rows and policies, and the ranked 236-target Contexto selector. Only
explicitly marked V89 dust/Mop history tests use it. Their original synthetic ID, rank 2,
typo-privacy and custom-fallback assertions remain unchanged. Fresh graph objects have
separate caches; live singleton caches clear before and after the scope. Every teardown
and a dedicated test prove native V90 Praf and the actual current ranked pack return.
V44's 235 mature-target and V89's 236-target/Mop boundaries are explicit snapshots; current
proxy anchors still must reach every current selectable target.

[Measured current pins](measured-pins.json) and [delta counts](delta-summary.json) record the
reviewed literals. The [measurement script](measure_pins.py.txt), [inverse generator](generate_delta.py.txt)
and [literal updater](update_current_pins.py.txt) preserve the procedure. None writes a
served fixture, runtime module or default pin. The initial artifact generation and focused
run both completed successfully before these evidence archives were packaged.
