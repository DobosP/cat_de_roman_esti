# Task result — V37 pilot lobby

## Summary

- Reordered the lobby to Alchimie, Conexiuni, Cald sau Rece, then Lanțul Cuvintelor.
- Marked only Alchimie with the terse `Începe aici` badge and a restrained accent border/glow.
- Hid the entire history/export/totals section while local play count is zero; it returns unchanged after a completed play.
- Added focused source-contract coverage for order, the single highlight, absence of board-rank metadata, history suppression, and the existing responsive grid.
- Rebuilt the tracked Vite release bundle.

## Files changed

- `frontend/src/games.ts`
- `frontend/src/screens/Home.tsx`
- `frontend/src/styles/arcade.css`
- `frontend/tests/v37-pilot-lobby.test.mjs`
- `cat_de_roman_esti/web/static/index.html`
- `cat_de_roman_esti/web/static/.vite/manifest.json`
- Hashed files under `cat_de_roman_esti/web/static/assets/` regenerated by Vite; the four tracked font subsets are unchanged.

## Verification

- `npm test` — 13/13 test files passed.
- `npm run lint` — passed with no findings.
- `npm run typecheck` — passed.
- `npm run build` — passed; initial JS/CSS gzip is **117.17 KiB / 120 KiB** and exactly four Romanian-capable Latin/Latin Extended font subsets were retained.
- `PYTHONPATH=. /home/dobo/work/cat_de_roman_esti/.venv/bin/python -m pytest -p no:cacheprovider tests/test_static_asset_cache.py tests/test_web.py -q` — 14 passed.
- `PYTHONPATH=. /home/dobo/work/romania_scraper/.venv/bin/python -m pytest tests/test_wordgames_session_store.py -q` — 16 passed (only the known pytest config/cache warnings in this read-only worktree environment).
- `git diff --check` — passed with no output.

The first build attempt used a temporary dependency symlink and was rejected by the font-source path guard; installing the same lockfile dependencies locally with `npm ci --offline` fixed the environment, and the final build above is green. The first static/web pytest attempt used the mandated scraper venv, which lacks Django; it was rerun with the repo's Django 5.2 venv and passed 14/14.

## Risks / manual review

- The game-order and highlight are explicitly provisional pilot guidance, not a numeric board-quality promise.
- No browser screenshot was captured; source contracts preserve the existing phone grid, and the production bundle/build gates are green.
- No API, score, session, board-selection, STATUS, or ADR files were changed in this slice.

## Merge recommendation

Recommended for integration. Commit: `ac5c5a7` (`feat(lobby): prioritize V37 pilot games`). The slice is narrow, deterministic, frontend-only, and green against its required gates.
