Valid until: the reviewed content, Lanț runtime, React screen, CSS or built assets change — then repeat affected checks.

Independent interaction review by `/root/v94_pack_author`. This agent authored the pack proposals and independently reviewed quick content; it did not author the caption registry. The small earned-trail presentation fix below was implemented by this reviewer after reproducing the problem, so its own checks are implementation verification rather than a separate second-person approval of that CSS change.

Before evidence on main’s port 8150 shows the actual weak guidance: Cluj-Napoca’s Salina Turda and Transilvania choices both say only “legătură directă”; Eminescu’s Junimea hint says “parte și întreg”. Those words do not explain the Romanian association. `before.json` and the six `before-*.png` captures record the actual responses and DOM at 320px with a 32px root font (200%). The new caption work should explain city/region, society/writer, journal/poet and ingredient/dish relationships without requiring players to infer the old graph’s direction.

The same captures reproduce a separate reading problem in earned history: the Cluj trail occupies 1564px and the Eminescu trail 1197px inside a 262px container, with a hidden horizontal scrollbar. A player sees the initial word and a fragment of the first arrow instead of the route they earned. The narrow fix removes the Lanț-only nowrap strip and lets relation phrases and their destination chips wrap inside coherent steps. It keeps the existing focusable trail and all game actions, state, recovery and persistence. The new regression requires every earned caption and node to fit without internal horizontal clipping at 320px/200%, with keyboard movement, GET and reload checks. Results and final hashes will be recorded after the real rebuilt frontend passes.

The rejected `lt_geografie_242` proposal exposed a content-to-interface boundary error. Contrary to an initial review assumption, Lanț’s `_state` includes `target.description` and `Lant.tsx` renders it under the target. The frozen Crucea Caraiman description claims a 28m monument on Șaua Caraiman; the monument owner’s [MApN history](https://reabilitare-crucea-caraiman.mapn.ro/pages/istoric), independently opened on 2026-09-15, gives 39.3m total and the 2291m secondary summit on the southeast side. Root’s final factual check caught this and rejected the new round. No source/graph repair or new-level approval is implied by retaining four independently reviewed relationship captions for existing edges. The regression suite therefore covers the two old V93 rounds and the newly accepted culinary round, not the rejected geographic level.

Final verification: **22 browser checks passed in 29.3 seconds** on a fresh V94 server at port 8152. Eight distinct shortest routes were completed on desktop and mobile; three hint journeys on each project checked direction, alternatives, exact-hop activation, persistence and focus. The mobile project explicitly asserts 320px viewport and 32px root text. Every displayed earned node and relation fits its container without internal horizontal clipping; the trail remains reachable and visibly focused using Tab. The final captures show the posthumous Academy relationship and optional-rice relationship fully readable as wrapped steps. Before/after records, final source hashes and screenshot hashes are bound by `final-review.json`.

The first interrupted test run is retained. It incorrectly expected completed rounds to resume automatically and omitted the concrete place name Turda from its semantic assertion. The corrected test checks reload after the first earned hop, before terminal state, and separately verifies terminal GET and the visible winning path. No runtime behavior was changed to satisfy those two test assumptions. The final extra pass adds explicit trail-focus and font-size assertions. Completed games still use the existing resume policy.
