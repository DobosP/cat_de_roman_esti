# V90 complete before/after impact

Valid until: bound runtime/data change — then repeat affected evidence; otherwise keep as history.

The [baseline receipt](baseline-receipt.json) binds clean local main190d7fd and the
[final receipt](final-receipt.json) binds V90 after two promotions. Each complete capture
makes16,800 fresh Contexto requests:240 old approved records ×70 declared words. No
observations are reused. The capture retains selected HTTP/status, confirmation and guess
fields, not whole HTTP bodies or generated game IDs. Raw captures, exact comparison and command output are preserved
losslessly with zero gzip timestamps and both decoded/archive hashes. Scripts and inputs
are included; [the compact comparison](comparison-summary.json) names every changed scope.

**11,120 selected feedback observations remain exact;5,680 change.** Of the changes,2,880 are newly recognized
forms/concepts (12 normalized surfaces ×240records);240 migrate Praf's synthetic ID to
its native identity;2,385 retain rank and change other fields, mostly graph-population
closeness (2,365 cases) and20 temperature changes;8 existing-identity responses have a lower rank and167 a higher rank. No won
flag changes. These categories are measurements, not a claim that every change is better.
Praf's old Pământ fallback is deliberately retired; its unrelated-target ranks often cool.
For old Mop, native bread/biscuit cues become warmer through the crumb/cleaning graph.
Other powder owners stay distinct; prafuri and17 remaining unknown stress words do not
silently acquire owners. The three new nouns are not approved hidden targets themselves.

All659 old pack records remain exact. All83 Alchimie books/profiles,100 Lanț route
profiles and336 complete derived rows/payloads remain exact. Existing eligibility,
preferred and starter stock is retained. The17 reviewed directed links each support a
fresh one-move Lanț win;13 links involved a node absent from the baseline, and the other four
had no direct win before. This is link coverage, not17new curated rounds.

One nonshortest choice changes on `lt_stiinta_216`: Burete de vase replaces Aluat in
the shown water-neighbor menu. Both shortest choices, aer/Ploaie, remain; all206 old
shown shortest first hops survive within the same bounds. The old water/dough edge is
still present. No new Lanț board is claimed.

Thirteen old node degree fields regenerate from the new edges; all old semantic fields,
9,442 links,8,631 stored forms and180 CLI puzzles remain exact. The new plate/sponge
bridge opens exactly seven old kitchen nodes; its independent removal control is in
[the topology review](../plate-topology-review.md). Only Contexto's initial seeded payload
changes, with reachability2343→2353; the other five seeded starts remain byte-identical.

New serving inventory is661records,653approved and8pending; Contexto eligibility236→238.
There are3new concepts,17links,10stored grammatical forms and0reviewed synonyms. Burete
and the Conexiuni/Lanț candidate scouts remain held. Full integration and agent source/
artifact review are still separate from this impact measurement and human playtesting.
