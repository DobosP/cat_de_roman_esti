import {chromium} from '/home/dobo/work/_worktrees/cat_de_roman_esti/feat__v92-entry-creation-03/frontend/node_modules/playwright/index.mjs';
import {readFileSync,writeFileSync} from 'node:fs';
import assert from 'node:assert/strict';
const root='/home/dobo/work/_worktrees/cat_de_roman_esti/feat__v92-entry-creation-03',out='/home/dobo/work/_temp/feat__v92-entry-creation-03/verifier/gui-final',base='http://127.0.0.1:8166',save='cat_alchimie_exploration_v1';
const browser=await chromium.launch({headless:true}),results=[];const checkpoint=JSON.parse(readFileSync(`${root}/frontend/e2e/alchimie-221-expanded-checkpoint.json`)).collection;
async function fresh(width=320,height=844){const context=await browser.newContext({baseURL:base,viewport:{width,height},locale:'ro-RO',reducedMotion:'reduce'});return {context,page:await context.newPage()};}
async function ready(page){await page.waitForFunction(()=>document.querySelector('.alchemy-explore-screen')?.getAttribute('aria-busy')==='false');await page.evaluate(()=>document.fonts.ready);}
function response(page,path){return page.waitForResponse(r=>r.request().method()==='POST'&&new URL(r.url()).pathname===path);}
async function capture(page,name){await page.screenshot({path:`${out}/${name}.png`});}
for(const cfg of [{width:320,height:844,scale:1},{width:390,height:844,scale:1},{width:320,height:360,scale:2}]){
 const {context,page}=await fresh(cfg.width,cfg.height);await page.goto('/alchimie');const wait=response(page,'/api/alchimie/explore');await page.getByRole('button',{name:'Începe explorarea →'}).click();const state=await(await wait).json();await ready(page);if(cfg.scale===2)await page.addStyleTag({content:'html { font-size:200% !important; }'});
 const first=page.locator('.alchemy-word').first();const label=await first.getAttribute('aria-label');await first.click();await page.locator('.alchemy-library-tools > summary').click();await page.getByRole('searchbox').fill('zzzz');await page.locator('.alchemy-library-tools > summary').click();assert.equal(await page.locator('.alchemy-word').count(),0);assert.equal(await page.getByRole('searchbox').isVisible(),false);
 const before=await page.evaluate(k=>localStorage.getItem(k),save);const reset=page.getByRole('button',{name:'Șterge căutarea',exact:true});const rect=await reset.boundingBox();assert(rect.width>=44&&rect.height>=44);await reset.scrollIntoViewIfNeeded();const posts=[];page.on('request',r=>{if(r.method()==='POST')posts.push(r.url());});await reset.focus();await page.keyboard.press('Enter');await page.waitForFunction(()=>document.querySelectorAll('.alchemy-word').length>0);assert.equal(await page.locator('.alchemy-word').count(),state.inventory.length);assert.equal(await page.locator('.alchemy-word[aria-pressed=true]').getAttribute('aria-label'),label);assert.equal(await page.evaluate(k=>localStorage.getItem(k),save),before);assert.equal(posts.length,0);assert.equal(await page.locator('.alchemy-library-tools').getAttribute('open'),null);
 const focus=await page.evaluate(()=>{const a=document.activeElement;const r=document.querySelector('.alchemy-inventory-panel h2').getBoundingClientRect();return {name:a.getAttribute('aria-label'),headingTop:r.top,headingBottom:r.bottom,height:innerHeight,overflow:document.querySelector('.alchemy-screen').scrollWidth>document.querySelector('.alchemy-screen').clientWidth};});assert.equal(focus.name,'Colecția ta');assert(focus.headingTop>=0&&focus.headingBottom<=focus.height&&!focus.overflow);await capture(page,`search-reset-${cfg.width}-${cfg.height}-${cfg.scale}`);results.push({case:'hidden_search_keyboard_clear',...cfg,rect,focus,posts:posts.length,save_unchanged:true,selection_retained:true});await context.close();
}
{
 const {context,page}=await fresh();const r=await page.request.post('/api/alchimie/explore',{data:{progress:checkpoint.progress}});assert(r.ok());let state=await r.json();assert.equal(state.inventory.length,221);assert.equal(state.discovered_count,117);assert.equal(state.complete,false);assert.equal(state.world.total_concepts,225);const crafted=[];
 while(!state.complete){const u=`/api/alchimie/explore/${state.game_id}`;await page.request.post(u+'/hint',{data:{}});state=await(await page.request.post(u+'/hint',{data:{}})).json();assert.equal(state.hint.stage,'pair');const[a,b]=state.hint.pair.map(x=>x.id);state=await(await page.request.post(u+'/combine',{data:{a,b}})).json();crafted.push(state.result.label);assert(crafted.length<=4);}
 assert.equal(state.inventory.length,225);assert.equal(state.discovered_count,121);assert.equal(crafted.length,4);await page.goto('/alchimie');await page.evaluate(({key,state})=>localStorage.setItem(key,JSON.stringify({version:1,game_id:state.game_id,revision:state.revision,progress:state.progress,goal_id:state.goal_id,compatible_recipe_hashes:state.compatible_recipe_hashes})),{key:save,state});await page.reload();await ready(page);assert.equal(await page.locator('.alchemy-word').count(),225);assert.equal(await page.locator('.alchemy-word:enabled').count(),0);assert(await page.locator('.alchemy-explore-complete').isVisible());assert.equal(await page.locator('.alchemy-craft-prompt').innerText(),'Răsfoiește cuvintele sau deschide „Rețetele mele”.');assert.equal(await page.getByRole('checkbox').count(),0);assert.equal(await page.getByRole('button',{name:'💡 O idee?',exact:true}).count(),0);assert(!(await page.locator('.alchemy-workspace').innerText()).includes('Continuă să descoperi'));await capture(page,'completed-225');const before=await page.evaluate(k=>localStorage.getItem(k),save);const posts=[];page.on('request',r=>{if(r.method()==='POST')posts.push(r.url());});await page.locator('.alchemy-library-tools > summary').click();await page.getByRole('searchbox').fill('pilaf');assert.equal(await page.locator('.alchemy-word').count(),1);assert((await page.locator('.alchemy-word').innerText()).includes('Pilaf de legume'));await page.locator('.alchemy-library-tools > summary').click();await page.getByRole('button',{name:'Șterge căutarea',exact:true}).click();assert.equal(await page.locator('.alchemy-word').count(),225);await page.locator('.alchemy-explore-journal > summary').click();assert.equal(await page.locator('.alchemy-journal-entry').count(),121);assert.equal(await page.evaluate(k=>localStorage.getItem(k),save),before);assert.equal(posts.length,0);await capture(page,'completed-225-journal');await page.reload();await ready(page);assert.equal(await page.locator('.alchemy-word').count(),225);results.push({case:'completed225_collection',restored_old_words:221,restored_old_discoveries:117,new_crafts:crafted,total:225,journal:121,save_unchanged:true,browse_posts:posts.length,reload_preserved:true});await context.close();
}
for(const scale of [1,2]){
 const {context,page}=await fresh();await page.goto('/conexiuni');await page.getByRole('button',{name:/^Joacă(?: →)?$/}).click();await page.locator('.connections-grid button').first().waitFor();if(scale===2)await page.addStyleTag({content:'html { font-size:200% !important; }'});await page.evaluate(()=>document.fonts.ready);const geometries=[];
 for(let i=0;i<=4;i++){if(i)await page.locator('.connections-grid button').nth(i-1).click();assert.equal(await page.locator('.next-move-progress').innerText(),`${i}/4`);const g=await page.locator('.connections-coach').evaluate(e=>({parent:e.getBoundingClientRect().toJSON(),parts:[...e.querySelectorAll(':scope > span:not(.next-move-icon)')].map(x=>({rect:x.getBoundingClientRect().toJSON(),scroll:x.scrollWidth,client:x.clientWidth})),overflow:document.querySelector('.connections-screen').scrollWidth>document.querySelector('.connections-screen').clientWidth}));assert(!g.overflow);for(const p of g.parts){assert(p.rect.width>35&&p.rect.left>=g.parent.left&&p.rect.right<=g.parent.right+.1&&p.scroll<=p.client+1);}for(let a=0;a<g.parts.length;a++)for(let b=a+1;b<g.parts.length;b++){const x=g.parts[a].rect,y=g.parts[b].rect;assert(!(x.left<y.right&&y.left<x.right&&x.top<y.bottom&&y.top<x.bottom));}geometries.push({selected:i,...g});}
 await capture(page,`conexiuni-320-${scale}`);results.push({case:'conexiuni_coach',width:320,scale,geometries});await context.close();
}
for(const width of [320,390])for(const game of [{key:'contexto',path:'/cald-rece',input:'Concept de ghicit'},{key:'lant',path:'/lant',input:'Următorul concept'}]){
 const {context,page}=await fresh(width);await page.goto(game.path);await page.getByRole('button',{name:/^Joacă(?: →)?$/}).click();await page.getByRole('textbox',{name:game.input}).waitFor();await page.evaluate(()=>document.fonts.ready);const controls=await page.locator('.screen-pad button').evaluateAll(xs=>xs.filter(x=>x.checkVisibility()).map(x=>({label:x.innerText,rect:x.getBoundingClientRect().toJSON()})));for(const c of controls)assert(c.rect.height>=44&&c.rect.width>=44,`${game.key} ${c.label} ${JSON.stringify(c.rect)}`);await capture(page,`${game.key}-${width}-targets`);results.push({case:'control_targets',game:game.key,width,controls});await context.close();
}
await browser.close();writeFileSync(`${out}/session03-gui.json`,JSON.stringify({passed:true,results},null,2)+'\n');console.log(JSON.stringify({passed:true,cases:results.length,complete:results.find(x=>x.case==='completed225_collection')},null,2));
