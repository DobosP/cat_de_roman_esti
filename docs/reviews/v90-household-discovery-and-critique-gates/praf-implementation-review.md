# V90 native Praf — actual implementation review

Valid until: a bound implementation changes — then re-review; artifact observations describe the coherent staged V90 state.

**Accepted as implemented.** A whole-module AST comparison against the V89 baseline,
with only the two authorized transformations, matches the current projection module:
exclude native `praf` and remove its dead three-target neighborhood. Contexto engine,
feedback and service files remain byte-identical. All other464 projection payloads,
26domains,4native audit tuples,11exact native pairs and71legacy proxies remain unchanged.

The actual applied graph contains exactly3new concepts/17directed links/10forms. All
2,413old node semantic records and9,442old edges are unchanged. Thirteen old degree
fields are regenerated metadata; every increment equals the added edge endpoints.
All13,457old node-ID/label/form ownership checks remain exact. Dust and the food powders
retain separate identities; the synthetic dust ID and Pământ fallback are retired.

Fresh actual Django BFF checks pass82first guesses,26journey requests,6nonwinning typo
privacy probes,3native dust exact wins and3missing-owner custom-graph probes, plus a
normal public create. Dust forms share one attempt, stay nonwinning against tool targets,
and preserve paid clue/resume/exact plural wins. Native dust feedback is4 for Făraș and
Aspirator, replacing the old scoped2 without a compensating override. A custom graph
without dust returns unknown/zero attempts instead of the retired approximation.
Private dust-target tests establish answer boundaries, not public target eligibility.

[The exact review](praf-implementation-review.json) and
[verification receipt](final-analyst/verification.json) bind actual sources, scripts,
lossless logs and measurements. Three reviewer preflight mistakes remain archived:
two AST-container assumptions and an overbroad old-node equality check corrected to
explicitly verify regenerated degree metadata. No application/test assertion was changed.

[The final portable analyst judgment](analyst.json) promotes exact pending355 Făraș and
356 Aspirator after full C1–C6 assessment. This review does not claim the all240old-target
impact matrix, final postpromotion artifacts, full integration, real devices or human
playtesting. Those remain required before landing. Burete and the three new-node hidden
targets remain held; all earlier judgments retain their original bytes.
