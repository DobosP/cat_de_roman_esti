"""Independent raw quality review; keep authorizes pending staging only."""
import hashlib
import json
from pathlib import Path

D = Path(__file__).resolve().parent
probe = D / 'independent-quality-probe.json'
probe_sha = hashlib.sha256(probe.read_bytes()).hexdigest()
decisions = {
    'gastronomie': [
        ('contexto[0]', 'Ciorbă de perișoare is a concrete familiar dish with a dominant sense, independently documented by long-standing recipe authors. Five direct native approaches—ciorbă, carne, orez, leuștean and chiftele—are recognizable and all returned hot in my independent BFF probe. Naive mâncare is hot at rank 7 and supă warm at 12. Chiftele is a related minced-meat preparation, not a claim that fried meatballs are an ingredient; rice is a familiar variant, not a universal requirement. Masă is only lukewarm at 100, a bounded limitation outweighed by several ordinary strong openings. The target is unused. C1–C6 pass for easy pending staging.',
         ['https://www.barbatlacratita.ro/2010/10/ciorba-de-perisoare.html', 'https://jamilacuisine.ro/ciorba-de-perisoare-reteta-video/']),
        ('lant[0]', 'The two independently replayed shortest routes bridge Ciorbă de perișoare to Sarmale through Orez or Carne. Both are familiar ingredient associations linking different dishes, with two real first-hop choices and no single intermediate choke point. The two-hop length suits easy. Recipes with rice and meat directly support the route without assuming all variants use both. The live captions remain generic, which is a copy-improvement opportunity; the existing edge predicates and ordinary ingredient identities make the route legible. D1–D5 pass for pending staging; actual allocated-ID and selection checks remain required.',
         ['https://www.barbatlacratita.ro/2010/10/ciorba-de-perisoare.html', 'https://jamilacuisine.ro/sarmale-pas-cu-pas-reteta-video/']),
    ],
    'geografie': [
        ('lant[0]', 'A natural rock formation and a built memorial are distinct recognizable endpoints on one well-known mountain plateau. The park administration identifies Sfinx and Babele among its best-known attractions and lists the Heroes’ Cross; this resolves the stored Sfinx salience 0.50 warning without treating obscurity as difficulty. I independently replayed both two-hop routes: via neighboring Babele and via the specific Bucegi massif. These are named local geographic relationships, not arbitrary country co-occurrence or a taxonomy-only ladder. Babele and the massif supply two distinct first hops without a single compulsory intermediate. Short scenic familiarity fits easy and the natural-to-built arc is sufficient for that band. D1–D5 pass for pending staging. Generic public edge captions should be improved but do not falsify the content.',
         ['https://www.bucegipark.ro/ro/', 'https://www.bucegipark.ro/media/public_docs/management_plan/10_Oct_2024/Planul_de_Management_al_Parcului_Natural_Bucegi__-_v.2011.pdf']),
    ],
    'viata_de_roman': [
        ('conexiuni[0]', 'All four predicates are strict and recognizable. Picior uses the everyday whole-lower-limb sense, so heel, thigh, calf and knee all qualify. Carte, Revistă, Ziar and Abecedar are ordinary printed publications; the book subtype stays within its own group. An, Secol, Săptămână and Lună calendaristică are precise calendar intervals, not a vague thematic abstraction cluster. The final group uses the conventional preparation/cleaning verb a bate: carpet cleaning, beating egg whites, whipping cream and flattening schnitzel meat during preparation. The explicit preparation clause prevents a false claim about striking finished food; ordinary Romanian calls this preparing/bating șnițele and frișcă. The anatomical words do not conventionally undergo the named preparation/cleaning operation; arbitrary physical hitting is not a defensible competing predicate. A playing-card sense of carte does not complete an alternative partition. Three clear anchors and one shared-verb trick fit normal, with no obscure members, mirrors or label self-leakage. Fresh strict lints show no contested or raw-unfair tiles; I solved all four groups through the BFF. Historical raw overlap checks show no prohibited repeated quartet. B1–B9 pass; keep for pending staging only.',
         ['https://dexonline.ro/definitie/bate/definitii', 'https://www.oetker-professional.ro/retete/tortul-padurea-neagra', 'https://www.lauralaurentiu.ro/retete-culinare/retete-mancare/snitel-din-piept-de-pui-in-pesmet-reteta-simpla.html']),
        ('contexto[0]', 'Temă pentru acasă denotes a specific assigned school task that pupils, parents and teachers recognize, not a vague abstract theme. Its full phrase avoids the polysemy of bare temă. Five direct native approaches are ordinary school objects/actions: notebook, pencil, lesson, learning and classroom board. My independent BFF probes put școală hot at 7, caiet/creion/lecție hot at 2, and both elev/profesor warm at 28/30. Thus an uninformed opening has coherent progress rather than a cold near-neighbor trap. The national ministry’s discussion independently establishes the familiar school practice; the game makes no claim about current homework regulation. No target duplication or neighborhood-floor issue remains. C1–C6 pass for easy pending staging.',
         ['https://www.edu.ro/press_rel_208_2025']),
    ],
}
for category, rows in decisions.items():
    path = D / 'candidates' / category / 'candidates.json'
    candidate_sha = hashlib.sha256(path.read_bytes()).hexdigest()
    data = json.loads(path.read_bytes())
    expected = {f'{kind}[{i}]' for kind in ('conexiuni', 'contexto', 'lant', 'alchimie')
                for i, _ in enumerate(data[kind])}
    assert {ref for ref, _, _ in rows} == expected
    result = {
        'category': category, 'candidate_sha256': 'sha256:' + candidate_sha,
        'reviewer': 'v94-independent-pack-quality-world-author',
        'coverage_note': 'Full B/C/D and universal rubric walk against exact raw candidates, native descriptions and source predicates. Independent fresh strict prospective lint and fixed-content BFF replays are bound below. keep permits pending staging only; actual allocated-ID analyst/verifier and natural-selection gates remain required.',
        'instances': [{'ref': ref, 'verdict': 'keep', 'note': note, 'sources': sources}
                      for ref, note, sources in rows],
        'independent_probe_sha256': probe_sha,
    }
    output = path.parent / 'verify_quality.json'
    output.write_text(json.dumps(result, ensure_ascii=False, indent=2) + '\n')
    print(category, hashlib.sha256(output.read_bytes()).hexdigest())
