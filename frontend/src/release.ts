// Testers copy the lobby badge into feedback, so it carries the exact patch version.
// tests/test_release_version_consistency.py keeps these equal to the package versions.
export const RELEASE_VERSION = "1.0.1";
export const RELEASE_LABEL = "V1.0.1";
