Valid until: final V91 integration supersedes this incomplete run — then treat as history.

The first Python3.12 full run showed two stale Sport recipe expectations. The root deliberately sent SIGINT to its identified pytest process after independent reproduction and a passing bounded correction. The runner exited2 after580.72seconds; this incomplete red run is excluded from the final green gate. Runtime, content and frontend inputs remained unchanged. The original freeze, log and receipt are preserved here.
